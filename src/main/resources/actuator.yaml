management:
  endpoint:
    loggers:
      enabled: true
    health:
      enabled: true
    info:
      enabled: true
    metrics:
      enabled: true
    prometheus:
      enabled: true

#      cors:
#        allowed-origins: "*"
#        allowed-methods: GET